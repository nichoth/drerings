# Human Test Plan: 2026-05-16-stamps (Stamps Remediation)

Generated by `ed3d-plan-and-execute:test-analyst` after final code review.
Branch: `004-stamps`. BASE `87f8640` → HEAD `02c657f`.

## Coverage Summary

Automated criteria: 51 | Covered: 49 | Partial (delegated to manual): 2

| Partial | Reason | Manual coverage |
|---------|--------|-----------------|
| stamps.AC5.1 success panel | Implementation present (`src/routes/send.ts:167-178`) but no automated assertion on the success-state DOM | HV-5 / Phase 1.4 |
| stamps.AC9.4 TRUNCATE | Documented in migration comment, no automated assertion | Phase 9.4 |

Pre-existing baseline failures unrelated to this work (unchanged from BASE
SHA, files not modified by this branch):
- `test/us007-free-user-drawing.test.ts`
- `test/us013-pricing-page.test.ts`

## Prerequisites

- Local DB seeded: `npx netlify db migrations apply`
- For integration-gated assertions: `RUN_DB_INTEGRATION=1 npx vitest run` green
- `npx vitest run` shows only the two pre-existing failures noted above
- `npm run lint` passes
- Resend account with sandbox API key + Svix webhook per HV-6
- Autumn sandbox account (or `shouldUseMockCheckout()` returning true via
  missing `AUTUMN_SECRET_KEY`)
- `npm start` running locally; app reachable at `http://localhost:8888`
- Personal Gmail inbox and personal Apple Mail inbox for HV-1
- Working `psql` or `npx netlify db query` for HV-3 / HV-4

## Phase 1: Postcard Send Happy Path

| Step | Action | Expected |
|------|--------|----------|
| 1.1 | Sign in as a new test user via magic link | Signup grants 5 stamps; nav shows balance "5" |
| 1.2 | Draw a simple shape, add alt text "smoke test", press Save | Drawing saved; routed to `/drawing/<id>` |
| 1.3 | Click "Send postcard" CTA | Routed to `/send/<drawing_id>`; recipient email input visible; "Send postcard" button visible; "1 stamp" indicator appears in Send postcard actions group only (not Publish actions) |
| 1.4 | Enter your own email; click "Send postcard" | Form replaced by a success panel showing the recipient email and "4 stamps remaining"; "Send another" button present |
| 1.5 | Open inbox in Gmail (HV-1) | Email arrives with subject "<displayName> sent you a Drerings postcard"; PNG renders; no raw sender email shown |
| 1.6 | Repeat 1.5 in Apple Mail (HV-1) | Same as 1.5 |
| 1.7 | Refresh app; check balance | Balance reads "4" |
| 1.8 | Click "Send another" | Form returns; balance still "4" |

## Phase 2: Insufficient Stamps and Buy Modal

| Step | Action | Expected |
|------|--------|----------|
| 2.1 | From Phase 1 user (4 stamps), send 4 more postcards (or POST 4 times) until balance is 0 | Balance reads "0" |
| 2.2 | Attempt one more send | 402 handled in UI; `BuyPackModal` opens automatically; pack options listed |
| 2.3 | Close the buy modal without purchasing | Form remains; no balance change |

## Phase 3: Failed Send Refund (Synchronous)

| Step | Action | Expected |
|------|--------|----------|
| 3.1 | Reset balance (buy a Starter pack or use a fresh user) | Balance ≥ 1 |
| 3.2 | Send to a malformed address (`not-a-real-address`) — or temporarily set `RESEND_API_KEY` to an invalid value | UI shows inline `role="alert"` containing "your stamp has been refunded"; balance unchanged |
| 3.3 | `SELECT reason FROM stamp_transactions WHERE user_id='<id>' ORDER BY created_at DESC LIMIT 2` | Top two rows are `failed_send_refund` then `send` (newest first); original `send` row untouched |

## Phase 4: Bounce Webhook (Asynchronous Refund) — HV-2

| Step | Action | Expected |
|------|--------|----------|
| 4.1 | From staging, send a postcard to `nonexistent-${Date.now()}@gmail.com` | UI shows the success panel |
| 4.2 | Wait ~60s; refresh Resend dashboard | `email.bounced` event fired against the endpoint |
| 4.3 | `SELECT reason, reference_id FROM stamp_transactions WHERE reference_id='<postcard_id>'` | Two rows: original `send` and a new `failed_send_refund` with the same `reference_id` |
| 4.4 | `SELECT status FROM postcards WHERE id='<postcard_id>'` | `failed_refunded` |
| 4.5 | Refresh app | Balance returned to pre-send value |

## Phase 5: Webhook Security

| Step | Action | Expected |
|------|--------|----------|
| 5.1 | `curl -X GET https://<host>/api/webhooks/resend` | 405 `{"error":"method_not_allowed"}` |
| 5.2 | `curl -X POST https://<host>/api/webhooks/resend -d '{}'` (no svix headers) | 400 `{"error":"invalid_signature"}` |
| 5.3 | Resend dashboard → "Send a test" on the webhook | 400 `invalid_signature` (test events aren't signed with production secret); expected |

## Phase 6: Stamp Refund (Autumn Path)

| Step | Action | Expected |
|------|--------|----------|
| 6.1 | Buy a Starter pack via the buy modal | Autumn checkout opens; complete purchase; balance increases by pack size |
| 6.2 | Navigate to `/stamps` (or where the refund button lives) | Newly purchased lot listed as refundable |
| 6.3 | Click Refund on the unused Starter lot | Success toast; balance decreases by remaining count of that lot |
| 6.4 | Check Autumn sandbox dashboard | New refund record visible against the original checkout |
| 6.5 | `SELECT status, http_status FROM autumn_refund_attempts WHERE checkout_id='<co_*>' ORDER BY attempted_at DESC LIMIT 1` | `succeeded`, `200` |
| 6.6 | Click Refund a second time on the same lot | Server returns no-op; UI shows it as no longer refundable |

## Phase 7: 30-Day Gift Refund Boundary

| Step | Action | Expected |
|------|--------|----------|
| 7.1 | Send a gift via "Gift stamps" UI to another address | Gift created; appears in sender's "Sent gifts" list |
| 7.2 | While unused and within 30 days, click Refund | Refund succeeds; balance reflects refund cents |
| 7.3 | Use `psql` to set `created_at = now() - interval '31 days'` on a test gift lot | Refund button is hidden / disabled for that lot |
| 7.4 | Reset, drain one stamp from a gift (recipient sends a postcard), re-check | Lot status `in_use`; refund button hidden |

## Phase 8: Invariant Monitoring — HV-3

| Step | Action | Expected |
|------|--------|----------|
| 8.1 | `curl -X POST http://localhost:9999/.netlify/functions/verify-stamp-invariants` | `{usersChecked: N, driftCount: 0, drifts: []}`; no rows added |
| 8.2 | Corrupt one user: `UPDATE users SET stamps_balance = stamps_balance + 1 WHERE id='<test_user>'` | (setup) |
| 8.3 | Re-run 8.1 | `driftCount: 1`; one row in `stamp_invariant_alerts` with `resolved_at IS NULL` |
| 8.4 | Re-run 8.1 a second time without fixing | `driftCount: 1` again; NO new alert row (partial unique index) |
| 8.5 | `UPDATE stamp_invariant_alerts SET resolved_at = now() WHERE user_id='<test_user>'` | (resolution) |
| 8.6 | Re-run 8.1 | New alert row inserts (old one still resolved); proves AC10.5 in live Postgres |
| 8.7 | Run smoke-script SQL from phase_03 Task 3 in `npx netlify db query` | First INSERT succeeds, UPDATE-to-resolved succeeds, second INSERT succeeds, parallel unresolved INSERT raises unique violation |

## Phase 9: Append-only Triggers — Live DB

| Step | Action | Expected |
|------|--------|----------|
| 9.1 | `SELECT id FROM stamp_transactions LIMIT 1` | gives `<tx_id>` |
| 9.2 | `UPDATE stamp_transactions SET delta = 0 WHERE id = '<tx_id>'` | Error mentioning `append-only` and `UPDATE`; row unchanged |
| 9.3 | `DELETE FROM stamp_transactions WHERE id = '<tx_id>'` | Error mentioning `append-only` and `DELETE`; row unchanged |
| 9.4 | `TRUNCATE TABLE stamp_transactions RESTART IDENTITY CASCADE` against a throwaway local DB | Succeeds (fills AC9.4 gap) |

## Phase 10: Autumn Attempt Row Survives Rollback — HV-4

| Step | Action | Expected |
|------|--------|----------|
| 10.1 | Local DB + stub Autumn returning 200; temporarily corrupt wrapping `UPDATE stamp_lots` so the transaction rolls back | (setup) |
| 10.2 | Trigger `refundPurchasedStampLot` via UI | UI reports an error; transaction rolls back |
| 10.3 | `SELECT reason FROM stamp_transactions WHERE user_id='<test_user>'` | No `refund` row (rollback worked) |
| 10.4 | `SELECT status FROM autumn_refund_attempts WHERE checkout_id='<co_test>'` | `succeeded` (forensic log persisted across the rollback) |
| 10.5 | Run Phase 8.1 | Drift detected and recorded for `<test_user>` |

## End-to-End: Full Happy-Path — HV-5

Purpose: confirm UI + API + Resend + bounce webhook + Autumn + invariants
all integrate end-to-end.

1. Sign in as a new user → balance is 5 (signup grant)
2. Phase 1.2–1.7 (send a real postcard; HV-1)
3. Phase 3.2–3.3 (malformed address → 502 + refund message)
4. Phase 6 (buy and refund a Starter pack)
5. Phase 4 (send to known-bad address; HV-2)
6. Phase 8.1 (`curl` verify-stamp-invariants) → `{driftCount: 0}`, no new alerts

Expected: every step succeeds and the final invariant run is clean.

## End-to-End: Resend Webhook Operational Setup — HV-6

Deployment prerequisite for AC6.* in production. Cannot be automated.

1. In Resend dashboard, add webhook endpoint `https://<host>/api/webhooks/resend`
2. Subscribe to `email.bounced`
3. Copy the `whsec_...` secret into `RESEND_WEBHOOK_SECRET` on Netlify
4. Click "Send a test" → expect 400 `invalid_signature` (correct)
5. Trigger a real bounce per Phase 4 / HV-2 → expect 200 and refund

## Human Verification Required (from test-requirements.md)

| Criterion | Why Manual | Where |
|-----------|------------|-------|
| HV-1 (AC1.1 real-world) | Resend's 200 only means accepted, not delivered/rendered | Phase 1.5–1.6 |
| HV-2 (AC6.1 real-world) | Webhook + Svix + bounce classification all live outside test fabric | Phase 4 |
| HV-3 (AC10.5 real-world) | Partial unique index + `ON CONFLICT` predicate against live Postgres | Phase 8 |
| HV-4 (AC15.6 real-world) | Connection-pool isolation across an actual ROLLBACK | Phase 10 |
| HV-5 | Full UI → API → Resend → Autumn → invariants chain | End-to-End Full Happy-Path |
| HV-6 | Resend dashboard cannot be configured by code | End-to-End Resend Webhook Setup |
| AC5.1 success-panel sub-assertion | Implementation present but no automated assertion | Phase 1.4 |
| AC9.4 TRUNCATE | Documented in migration comment, no automated assertion | Phase 9.4 |

## Traceability (selected highlights)

Full traceability matrix in the analyst report. Selected highlights:

| AC | Automated | Manual |
|----|-----------|--------|
| AC1.1 | test/us030 AC1.1 | Phase 1, HV-1 |
| AC3.3 | test/us030 AC3.3 | Phase 3.3 |
| AC6.1 | test/us032 AC6.1 | HV-2 / Phase 4 |
| AC9.1, AC9.2 | test/us034 (gated) | Phase 9 |
| AC10.3 | test/us035 + integration | Phase 8.4 / HV-3 |
| AC13.2 | test/us037 AC13.2 | Phase 4 / HV-2 |
| AC15.5 (all 6 sub-cases) | test/us039 AC15.5 × 6 | Phase 6.6 (succeeded prior) |
| AC15.6 | test/us039 AC15.6 (mock-structural) | HV-4 / Phase 10 |
