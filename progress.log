## Codebase Patterns
- `npm test` bundles and runs `test/index.ts` through `tapout`; Vitest tests
  live beside it and should be run directly with `npx vitest run <file>` for
  targeted checks.
- SPA state follows the template pattern: mutable app behavior is attached to
  the exported `State` function rather than scattered across components.
- The home route owns the Atrament canvas lifecycle and keeps drawing controls
  in component-local signals.
- Netlify Database migrations live under
  `netlify/database/migrations/<prefix>_<slug>/migration.sql`.
- Netlify Blobs helpers should call `getStore(...)` lazily inside exported
  operations, not at module load, so Netlify runtime context is available.
- Netlify Functions can live in nested directories under `netlify/functions`
  to match redirected API paths like `/api/auth/magic-link`.
- Shared backend helpers live under `netlify/lib/*`; keep function handlers
  thin and put reusable storage, email, session, and HTTP helpers there.
- One-time token consumption should be atomic in SQL with a guarded
  `UPDATE ... RETURNING`, checking both `used_at IS NULL` and expiry.
- WebAuthn passkey flows use `@simplewebauthn/server` in Netlify helpers and
  `@simplewebauthn/browser` in routes; challenge state is carried by a short
  lived signed `challenge_token` returned beside the generated options.
- Passkey API handlers are nested under
  `netlify/functions/auth/passkey/<flow>/<step>.ts` to match paths like
  `/api/auth/passkey/register/options`.
- Session-aware API handlers should use `getSession(event)` from
  `netlify/lib/session.ts`; it validates the signed cookie and queries the
  current `users` row so subscription state is not read from stale cookies.
- Frontend auth state is hydrated by `State.fetchAuthStatus(state)`, which
  calls `/api/whoami` and stores the result in `state.currentUser`.
- Routes that fetch authenticated data must react to
  `state.currentUser.value?.id`; auth hydration happens after initial render.
- Frontend paid/free checks should read
  `state.currentUser.value?.subscription_status`; tests that expect paid
  controls to be enabled must populate `state.currentUser`, not only
  `state.auth`.
- Drawing save/update requests are handled by
  `netlify/functions/drawings.ts`; item routes like `/api/drawings/:id`
  are parsed from `event.path` in the same function.
- Saved drawing persistence lives in `netlify/lib/drawings.ts`; API callers
  send JSON with a base64 `image`, `text`, and `alt_text`, and the helper
  converts the image to a `Blob` before writing Blob storage and DB rows.
- Saved drawing frontend behavior lives on `State`: use `State.SaveDrawing`,
  `State.FetchSavedDrawings`, and `State.OpenSavedDrawing` so route
  components do not own cross-route drawing state.
- `GET /api/drawings` and `GET /api/drawings/:id` return image data URLs
  assembled from Blob storage for thumbnails and reopening drawings.
- Saved drawing deletion is routed through `State.DeleteSavedDrawing`, which
  calls `DELETE /api/drawings/:id`, removes the item from
  `state.savedDrawings`, and clears `state.currentDrawing` if needed.
- Backend drawing deletion explicitly removes any `public_posts` row before
  deleting the drawing row and Blob image, even though the schema also has
  cascade behavior.
- Use `State.FetchSavedDrawing` when a route needs an owned drawing without
  navigation; use `State.OpenSavedDrawing` only for the Drawings -> composer
  flow because it intentionally routes back to `/`.
- Public posts are readable without a session through `GET /api/posts/:id`;
  frontend public-post loading goes through `State.FetchPublicPost`, and the
  `/post/:id` route owns document title and Open Graph metadata updates.
- Client route tests can render through `Router(state).match(path)` to verify
  route registration and component rendering together.

## 2026-05-09 22:16 PDT - US-001
Thread: https://ampcode.com/threads/
- Removed all AT Proto package dependencies from `package.json` and
  `package-lock.json`.
- Deleted the old Netlify auth and constellation functions, plus the feed,
  profile, and DID config modules that depended on the old backend.
- Simplified app state, login, routing, home publishing controls, and about
  copy so the app boots without AT Proto or Bluesky code paths.
- Added a US-001 regression guard that scans package manifests and source for
  removed backend references.
- Files changed: `package.json`, `package-lock.json`, `src/state.ts`,
  `src/util.ts`, `src/post-text.ts`, `src/index.ts`, `src/routes/home.ts`,
  `src/routes/index.ts`, `src/routes/login.ts`, `src/routes/login.css`,
  `src/routes/colophon.ts`, deleted old route/function/config files, and
  updated tests.
- Verification: `npx vitest run test/us001-no-atproto.test.ts
  test/home-route.color-picker.test.ts test/login-route.flows.test.ts
  test/state.flows.test.ts test/util.test.ts`; `npm run lint`; `npm test`;
  `npx tsc --noEmit`; browser smoke check at `http://127.0.0.1:8888/`
  confirmed the home route, canvas, and Save button render with no console or
  page errors.
- **Learnings for future iterations:**
  - The checked-in `test/test-bundle.js` is generated output if present; the
    tracked browser test entry point is `test/index.ts`.
  - Removing source modules also requires deleting or rewriting old Vitest
    tests because lint scans every `*.ts` file, not just test files that run.
  - Browser-use's Node REPL bridge was not exposed in this session, so the
    browser acceptance check used a Playwright fallback against the same local
    dev server.
  - `tsconfig.json` uses explicit `types` and `typeRoots`; Node-based tests
    need both `@types/node` and the `node` entry in `compilerOptions.types`.
  - `npm test` uses tapout's fixed browser-test port, so run it by itself
    instead of in parallel with other browser-oriented test commands.
---
## 2026-05-09 22:39 PDT - US-002
Thread: https://ampcode.com/threads/
- Added `@netlify/database` so the app has the official Netlify Database
  package available for later function work.
- Added the initial paid-accounts migration with `users`, `passkeys`,
  `magic_link_tokens`, `drawings`, and `public_posts`.
- Added a US-002 Vitest guard that checks the Netlify migration layout,
  required package, table columns, key constraints, and URL-facing
  `bigserial` public post id.
- Marked US-002 as passing in `specs/prd.json`.
- Files changed: `package.json`, `package-lock.json`,
  `netlify/database/migrations/0001_paid_accounts_schema/migration.sql`,
  `test/us002-netlify-db-schema.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us002-netlify-db-schema.test.ts`;
  `npx tsc --noEmit`; `npm run lint`; `npm test`.
- Local migration apply note: `npx netlify db status` requires a linked
  Netlify site, the installed CLI does not expose the current GA
  `database migrations apply` command, and the local Docker daemon was not
  running. The migration contract is covered by the static schema guard.
- **Learnings for future iterations:**
  - Current Netlify Database docs use migration directories named
    `<prefix>_<slug>` with a nested `migration.sql` file.
  - This repo is not linked to a Netlify site, so `netlify db status` cannot
    verify database setup from a fresh checkout.
  - Updating the Netlify CLI to the latest major currently conflicts with
    Vitest's optional OpenTelemetry peer dependency.
---
## 2026-05-09 22:44 PDT - US-003
Thread: https://ampcode.com/threads/
- Added `@netlify/blobs` as an app dependency.
- Added `netlify/lib/drawing-images.ts` with the named `drawings` blob store
  helper functions: `putDrawingImage`, `getDrawingImage`, and
  `deleteDrawingImage`.
- Added a US-003 Vitest contract covering stable blob keys, store selection,
  image reads, missing images, and deletion.
- Marked US-003 as passing in `specs/prd.json`.
- Files changed: `package.json`, `package-lock.json`,
  `netlify/lib/drawing-images.ts`, `test/us003-drawing-blobs.test.ts`,
  `specs/prd.json`, and `progress.log`.
- Verification: `npx vitest run test/us003-drawing-blobs.test.ts`;
  `npx tsc --noEmit`; `npm run lint`; `npm test`.
- **Learnings for future iterations:**
  - Use `@netlify/blobs` from `netlify/lib/*` modules for backend storage
    helpers.
  - Call `getStore('drawings')` inside each helper operation so the store is
    created after Netlify has populated its runtime context.
  - Drawing image blob keys use
    `users/<userId>/drawings/<drawingId>.png`.
---
## 2026-05-09 22:52 PDT - US-004
Thread: https://ampcode.com/threads/
- Added magic-link auth functions for
  `POST /api/auth/magic-link` and
  `GET /api/auth/magic-link/callback`.
- Added backend helpers for magic-link token creation/consumption, Resend
  delivery, signed session cookies, and shared HTTP JSON/origin parsing.
- Replaced the login placeholder with a single email form, Send link button,
  inline error handling, and a check-your-email success state.
- Added US-004 regression coverage for the API handlers, auth helpers, Resend
  payloads, and login route flow.
- Marked US-004 as passing in `specs/prd.json`.
- Files changed: `netlify/functions/auth/magic-link.ts`,
  `netlify/functions/auth/magic-link-callback.ts`,
  `netlify/lib/auth-store.ts`, `netlify/lib/http.ts`,
  `netlify/lib/resend.ts`, `netlify/lib/session.ts`,
  `src/routes/login.ts`, `src/routes/login.css`,
  `test/us004-auth-helpers.test.ts`,
  `test/us004-magic-link-api.test.ts`,
  `test/login-route.flows.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us004-auth-helpers.test.ts
  test/us004-magic-link-api.test.ts test/login-route.flows.test.ts`;
  `npx tsc --noEmit`; `npm run lint`; `npm test`; browser fallback
  against `http://127.0.0.1:8889/login` confirmed one Sign In heading, one
  Email input, one Send link button, and no page errors. Browser Use's Node
  REPL bridge was not exposed, and Computer Use was blocked by macOS
  authentication, so the browser check used Playwright against the local dev
  server.
- **Learnings for future iterations:**
  - Netlify Functions route cleanly from `/api/*` when nested under
    `netlify/functions`, for example `auth/magic-link.ts`.
  - `@netlify/database` exposes `getDatabase().pool.query(...)`; use
    parameterized SQL for helper modules under `netlify/lib`.
  - Magic-link login currently upserts a user before inserting the token
    because `magic_link_tokens.user_id` is non-nullable.
  - Resend delivery uses `RESEND_API_KEY`, with optional
    `RESEND_FROM_EMAIL`; no Resend SDK dependency is required.
  - Session cookies require `SESSION_SECRET` outside test/local Netlify mode.
---
## 2026-05-09 23:03 PDT - US-005
Thread: https://ampcode.com/threads/
- Added passkey registration and sign-in endpoints for options and verify
  steps under `/api/auth/passkey/*`.
- Added shared WebAuthn helpers for signed challenge tokens, registration
  verification, authentication verification, passkey storage, and counter
  updates.
- Added session cookie parsing for endpoint authorization without exposing the
  US-006 `getSession`/`whoami` contract yet.
- Added passkey sign-in to the login route, with magic-link-only fallback when
  WebAuthn is unavailable.
- Added a settings route with a logged-in-only "Register a passkey" action.
- Marked US-005 as passing in `specs/prd.json`.
- Files changed: `package.json`, `package-lock.json`,
  `netlify/lib/session.ts`, `netlify/lib/passkeys.ts`,
  `netlify/functions/auth/passkey/register/options.ts`,
  `netlify/functions/auth/passkey/register/verify.ts`,
  `netlify/functions/auth/passkey/login/options.ts`,
  `netlify/functions/auth/passkey/login/verify.ts`,
  `src/routes/login.ts`, `src/routes/login.css`,
  `src/routes/settings.ts`, `src/routes/settings.css`,
  `src/routes/index.ts`, `test/us005-passkey-helpers.test.ts`,
  `test/us005-passkey-api.test.ts`, `test/us005-passkey-ui.test.ts`,
  `specs/prd.json`, and `progress.log`.
- Verification:
  `npx vitest run test/us005-passkey-helpers.test.ts
  test/us005-passkey-api.test.ts test/us005-passkey-ui.test.ts`;
  `npx vitest run test/login-route.flows.test.ts test/state.flows.test.ts
  test/us004-magic-link-api.test.ts test/us004-auth-helpers.test.ts`;
  `npx tsc --noEmit`; `npm run lint`; `npm test`; browser fallback against
  `http://127.0.0.1:5173/login` and `/settings` confirmed the passkey button,
  signed-out settings state, and no console or page errors.
- Browser Use note: the required Node REPL tool was not exposed in this
  session, so browser verification used Playwright against the local Vite dev
  server.
- **Learnings for future iterations:**
  - SimpleWebAuthn v13 returns credential data as
    `registrationInfo.credential`; store its `id`, `publicKey`, and `counter`.
  - Existing `passkeys.public_key` is text, so encode public keys as base64url
    on write and decode to `Uint8Array` before WebAuthn authentication checks.
  - Until US-006 adds `getSession`, passkey registration can authorize with
    `readSessionUserFromCookie(event)` from `netlify/lib/session.ts`.
---
## 2026-05-09 23:09 PDT - US-006
Thread: https://ampcode.com/threads/
- Added `getSession(event)` in `netlify/lib/session.ts` to validate the
  signed session cookie, load the current `users` row, and return
  `{ user } | null`.
- Added `GET /api/whoami`, returning the current user's `id`, `email`, and
  `subscription_status`, or a 401 JSON error when signed out.
- Added `state.currentUser` and made `State.fetchAuthStatus(state)` hydrate
  auth state from `/api/whoami`; app boot now calls it from `src/index.ts`.
- Added US-006 regression coverage for session lookup, the whoami endpoint,
  and frontend auth hydration.
- Marked US-006 as passing in `specs/prd.json`.
- Files changed: `netlify/lib/session.ts`, `netlify/functions/whoami.ts`,
  `src/state.ts`, `src/index.ts`, `test/us006-session-whoami.test.ts`,
  `test/state.flows.test.ts`, `specs/prd.json`, and `progress.log`.
- Verification: `npx vitest run test/us006-session-whoami.test.ts
  test/state.flows.test.ts`; `npx tsc --noEmit`; `npm run lint`;
  `npm test`.
- **Learnings for future iterations:**
  - Do not authorize future paid APIs from the cookie payload alone; use
    `getSession(event)` so user and subscription data come from the DB.
  - `State.fetchAuthStatus(state)` is the frontend boot hydration point for
    `state.currentUser`; keep auth mutations attached to `State`.
---
## 2026-05-09 23:16 PDT - US-007
Thread: https://ampcode.com/threads/
- Added the free-account home warning with a `/pricing` upgrade link while
  keeping the drawing canvas available to anonymous visitors.
- Gated Save and Send It behind an active subscription, with accessible
  explanatory tooltip copy and a `/pricing` link for free/anonymous users.
- Added US-007 Vitest coverage for anonymous canvas loading, no
  localStorage/backend persistence, disabled paid controls, and pricing links.
- Updated existing home-route validation tests to model a paid user through
  `state.currentUser.subscription_status === 'active'`.
- Marked US-007 as passing in `specs/prd.json`.
- Files changed: `src/routes/home.ts`, `src/routes/home.css`,
  `src/components/button.ts`, `test/us007-free-user-drawing.test.ts`,
  `test/home-route.color-picker.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us007-free-user-drawing.test.ts`;
  `npx vitest run test/us007-free-user-drawing.test.ts
  test/home-route.color-picker.test.ts`; `npx tsc --noEmit`;
  `npm run lint`; `npm test`; browser fallback against
  `http://localhost:8889/` confirmed the canvas, warning, disabled Save,
  disabled Send It, pricing links, and no page JS errors or 5xx responses.
- Browser Use note: the required Node REPL browser tool was not exposed in
  this session, so browser verification used Playwright against `npm start`.
- **Learnings for future iterations:**
  - The Vite-only dev server proxies `/api/*` to the Netlify functions port,
    so browser checks that exercise auth hydration should use `npm start`.
  - Anonymous app boot intentionally receives a 401 from `/api/whoami`; treat
    that as signed-out state, not as a failed page render.
  - Home paid-control tests need `state.currentUser` populated with an
    `active` subscription when they expect Save to be enabled.
---
## 2026-05-09 23:25 PDT - US-008
Thread: https://ampcode.com/threads/
- Added the paid drawing API for `POST /api/drawings` and
  `PUT /api/drawings/:id`.
- Added drawing persistence helpers that decode base64 PNG data, write the
  image to Netlify Blobs, insert or update the `drawings` row, and delete the
  replaced blob when the blob key changes.
- Gated drawing save/update behind a current session with
  `subscription_status === 'active'`, returning 401 for anonymous users and
  402 for free users.
- Added US-008 Vitest coverage for session gating, paid saves, paid updates,
  Blob writes, DB writes, ownership-qualified updates, and replaced blob
  deletion.
- Marked US-008 as passing in `specs/prd.json`.
- Files changed: `netlify/functions/drawings.ts`,
  `netlify/lib/drawings.ts`, `test/us008-save-drawing-api.test.ts`,
  `test/us008-drawing-store.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us008-save-drawing-api.test.ts
  test/us008-drawing-store.test.ts`; `npx tsc --noEmit`; `npm run lint`;
  `npm test`.
- **Learnings for future iterations:**
  - One `drawings.ts` Netlify function can cover both collection and item
    writes by parsing the drawing id from `event.path`.
  - Save/update API tests can mock `netlify/lib/drawings` while lower-level
    persistence tests mock `@netlify/database` and `drawing-images`.
  - `json(...)` currently accepts `Record<string, unknown>`, so typed result
    interfaces should be converted to plain object literals in handlers.
---
## 2026-05-09 23:34 PDT - US-009
Thread: https://ampcode.com/threads/
- Added paid Save wiring on the home composer. It captures the canvas as a
  PNG data URL, POSTs or PUTs through `State.SaveDrawing`, and shows inline
  saved/error states.
- Added saved drawing read support for `GET /api/drawings` and
  `GET /api/drawings/:id`, backed by `listSavedDrawings` and
  `getSavedDrawing` in `netlify/lib/drawings.ts`.
- Added `/drawings` with thumbnails, text excerpts, update timestamps, Open,
  and placeholder Delete actions for the next story.
- Added cross-route drawing state so Open fetches the saved drawing, routes
  back to `/`, and repopulates text, alt text, and canvas image.
- Marked US-009 as passing in `specs/prd.json`.
- Files changed: `netlify/functions/drawings.ts`,
  `netlify/lib/drawings.ts`, `src/state.ts`, `src/routes/home.ts`,
  `src/routes/home.css`, `src/routes/drawings.ts`,
  `src/routes/drawings.css`, `src/routes/index.ts`, `src/style.css`,
  `test/us009-drawing-read-api.test.ts`,
  `test/us009-drawing-read-store.test.ts`,
  `test/us009-save-load-ui.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us009-drawing-read-api.test.ts
  test/us009-drawing-read-store.test.ts test/us009-save-load-ui.test.ts`;
  related targeted Vitest tests for US-007, US-008, home, and state;
  `npx tsc --noEmit`; `npm run lint`; `npm test`; Playwright browser smoke
  against `http://localhost:8889/` with mocked paid auth/drawing APIs.
- Browser Use note: the required Node REPL browser tool was not exposed in
  this session, so browser verification used the Playwright fallback already
  established in prior iterations.
- **Learnings for future iterations:**
  - Keep drawing reads in the existing `drawings.ts` Netlify function; parse
    the optional drawing id from `event.path` just like the update path.
  - Saved drawing images are sent to the SPA as PNG data URLs. This keeps the
    `/drawings` thumbnail and composer reopen flow simple until a dedicated
    image endpoint is needed.
  - Cross-route drawing UI state should live on `State`, not in route-local
    signals, because `/drawings` needs to hand an opened drawing to `/`.
  - Auth-dependent route effects should key off the current user id so they
    run when `/api/whoami` resolves after the first render.
---
## 2026-05-10 01:14 PDT - US-010
Thread: https://ampcode.com/threads/
- Added saved drawing deletion for active subscribers through
  `DELETE /api/drawings/:id`.
- Added backend storage deletion that verifies ownership, deletes any public
  post row, deletes the drawing row, and removes the drawing image blob.
- Enabled the `/drawings` Delete button with a native confirm prompt, inline
  error handling, immediate list removal, and current drawing cleanup.
- Added US-010 regression coverage for the storage helper, API handler, and
  Drawings route confirmation behavior.
- Marked US-010 as passing in `specs/prd.json`.
- Files changed: `netlify/lib/drawings.ts`,
  `netlify/functions/drawings.ts`, `src/state.ts`,
  `src/routes/drawings.ts`, `test/us010-delete-drawing-store.test.ts`,
  `test/us010-delete-drawing-api.test.ts`,
  `test/us010-delete-drawing-ui.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us010-delete-drawing-store.test.ts
  test/us010-delete-drawing-api.test.ts
  test/us010-delete-drawing-ui.test.ts`; neighboring drawing tests;
  `npx tsc --noEmit`; `npm run lint`; `npm test`; browser fallback against
  `http://127.0.0.1:5173/drawings` confirmed the delete confirm prompt,
  DELETE call, immediate list update, and no console or page errors.
- Browser Use note: the required Node REPL browser tool was not exposed in
  this session, so browser verification used Playwright against the local Vite
  dev server.
- **Learnings for future iterations:**
  - Keep destructive saved-drawing actions on `State` so routes update shared
    drawing signals consistently.
  - `DELETE /api/drawings/:id` returns 403 when the drawing is not owned by
    the current user.
  - Delete tests can mock `/api/whoami`, `/api/drawings`, and
    `/api/drawings/:id` in Playwright to exercise the authenticated
    `/drawings` route without a live database.
---
## 2026-05-10 01:23 PDT - US-011
Thread: https://ampcode.com/threads/
- Added the paid publish API at `POST /api/posts`, gated by
  `getSession(event)` and `subscription_status === 'active'`.
- Added `netlify/lib/posts.ts` with `publishDrawing`, which verifies drawing
  ownership and upserts `public_posts` so republishing returns the existing
  public post id.
- Added `/send/:drawingId` with an owner-only preview, Publish action, and
  redirect to `/post/:id` after a successful publish.
- Wired Send It buttons from the home composer and saved drawings list through
  `State.GoToSendDrawing`, and added `State.PublishDrawing` plus
  `State.FetchSavedDrawing` for route-owned data loading.
- Added US-011 regression coverage for the publish store helper, API handler,
  dynamic route matching, Send It navigation, preview loading, and publish
  redirect.
- Marked US-011 as passing in `specs/prd.json`.
- Files changed: `netlify/functions/posts.ts`, `netlify/lib/posts.ts`,
  `src/state.ts`, `src/routes/home.ts`, `src/routes/drawings.ts`,
  `src/routes/index.ts`, `src/routes/send.ts`, `src/routes/send.css`,
  `test/us011-publish-post-api.test.ts`,
  `test/us011-publish-post-store.test.ts`,
  `test/us011-send-route-ui.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us011-publish-post-api.test.ts
  test/us011-publish-post-store.test.ts test/us011-send-route-ui.test.ts`;
  neighboring drawing/home/state Vitest tests; `npx tsc --noEmit`;
  `npm run lint`; `npm test`; Playwright browser fallback against
  `http://127.0.0.1:5173/send/drawing-1` confirmed the preview image,
  Publish action, redirect to `/post/42`, and no page or console errors.
- Browser Use note: the required Node REPL browser tool was not exposed in
  this session, so browser verification used the established Playwright
  fallback against the local Vite dev server.
- **Learnings for future iterations:**
  - `State.OpenSavedDrawing` is navigation-oriented; routes that only need
    drawing data should call `State.FetchSavedDrawing`.
  - `POST /api/posts` lives in a separate Netlify function from drawing CRUD,
    with reusable publish storage in `netlify/lib/posts.ts`.
  - Publishing can be idempotent with `INSERT ... ON CONFLICT (drawing_id)`
    because `public_posts.drawing_id` is unique.
---
## 2026-05-10 01:29 PDT - US-012
Thread: https://ampcode.com/threads/
- Added public post read support to `GET /api/posts/:id`, with no session
  requirement and 404 JSON for unknown IDs.
- Added `getPublishedPost(postId)` to join `public_posts` to `drawings` and
  return image data URLs from Blob storage.
- Added `State.FetchPublicPost` and the `/post/:id` route to render the
  published image, text, alt text, and share metadata.
- Marked US-012 as passing in `specs/prd.json`.
- Files changed: `netlify/functions/posts.ts`, `netlify/lib/posts.ts`,
  `src/state.ts`, `src/routes/index.ts`, `src/routes/post.ts`,
  `src/routes/post.css`, `test/us012-public-post-api.test.ts`,
  `test/us012-public-post-store.test.ts`,
  `test/us012-public-post-ui.test.ts`, `specs/prd.json`, and
  `progress.log`.
- Verification: `npx vitest run test/us012-public-post-store.test.ts
  test/us012-public-post-api.test.ts test/us012-public-post-ui.test.ts`;
  `npx vitest run test/us011-publish-post-store.test.ts
  test/us011-publish-post-api.test.ts test/us011-send-route-ui.test.ts`;
  `npx tsc --noEmit`; `npm run lint`; `npm test`; Playwright browser
  fallback against `http://127.0.0.1:5173/post/42` confirmed the public post
  image, text, page title, Open Graph title/image, and no unexpected page or
  console errors.
- Browser Use note: the required Node REPL browser tool was not exposed in
  this session, so browser verification used the established Playwright
  fallback against the local Vite dev server.
- **Learnings for future iterations:**
  - Public post reads should not call `getSession`; the published URL is
    intentionally visitor-readable.
  - Public post image responses follow the saved-drawing pattern: read the
    drawing blob and return a `data:image/png;base64,...` URL.
  - Route-level metadata updates can live in the route component when the
    metadata depends on client-fetched content.
---
## 2026-05-10 01:37 PDT - US-013
Thread: https://ampcode.com/threads/
- Added the `/pricing` client route with free and paid tier details plus the
  `Subscribe - $5/month` CTA.
- Added signed-out checkout prompting so anonymous visitors see a `/login`
  link and a disabled Subscribe button before checkout exists.
- Added Pricing to the main navigation and covered the existing home route
  disabled Save/Send pricing link behavior in the US-013 tests.
- Marked US-013 as passing in `specs/prd.json`.
- Files changed: `src/routes/pricing.ts`, `src/routes/pricing.css`,
  `src/routes/index.ts`, `test/us013-pricing-page.test.ts`,
  `specs/prd.json`, and `progress.log`.
- Verification: `npx vitest run test/us013-pricing-page.test.ts`;
  `npx vitest run test/us013-pricing-page.test.ts
  test/us007-free-user-drawing.test.ts`; `npx tsc --noEmit`;
  `npm run lint`; `npm test`; Playwright browser fallback against
  `http://localhost:8889/pricing` confirmed the pricing route, tier headings,
  disabled signed-out Subscribe button, sign-in link, and no unexpected page
  or console errors.
- Browser Use note: the required Node REPL browser control surface was not
  exposed in this session, so browser verification used the established
  Playwright fallback against the local dev server.
- **Learnings for future iterations:**
  - `/pricing` is a client route for US-013; US-014 should attach checkout
    behavior to the existing Subscribe button instead of replacing the route.
  - Anonymous auth hydration still produces a known `/api/whoami` 401 in
    browser checks; intercept or allow that expected signed-out signal.
  - Route tests can render a route through `Router(state).match(path)` to
    cover dynamic route registration without importing the route component.
---
